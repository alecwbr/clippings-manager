{
  "name": "vue-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "start-api": "cd ../server && venv/bin/flask run --no-debugger",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test:unit": "vitest --environment jsdom",
    "test:pytest": "cd ../server && venv/bin/python -m pytest",
    "test:pytest-verbose": "cd ../server && venv/bin/python -m pytest -vv"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "pinia": "^2.0.17",
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.1",
    "@vue/test-utils": "^2.0.2",
    "jsdom": "^20.0.0",
    "sass": "^1.54.5",
    "vite": "^3.0.4",
    "vitest": "^0.21.0"
  }
}
